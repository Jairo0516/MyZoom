version: 0.2
phases:
  install:
    commands:
      #- yum install -y jq
      #- wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip
      #- unzip ./sonar-scanner-cli-4.7.0.2747-linux.zip
      #- export PATH=$PATH:/sonar-scanner-cli-4.7.0.2747-linux/bin/
      #- sudo amazon-linux-extras install java-openjdk11
      #- wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip
      #- unzip ./sonar-scanner-cli-4.7.0.2747-linux.zip
      #- export PATH=$PATH:./sonar-scanner-cli-4.7.0.2747-linux.zip/bin/
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip
      - unzip ./sonar-scanner-cli-4.7.0.2747-linux.zip
      - export PATH=$PATH:./sonar-scanner-4.7.0.2747-linux/bin/
  pre_build:
    commands:
  build:
    commands:
      #- cd /sonar-scanner-4.7.0.2747-linux/bin/sonar-scanner-4.7.0.2747-linux/bin
      - sonar-scanner -Dsonar.organization=my_zoom -Dsonar.projectKey=my_zoom -Dsonar.sources=/myzoom/src -Dsonar.host.url=http://44.203.62.27:9000 -Dsonar.login=883f27f4446229b809f8b178b84ec25b518a4cb2
      - sleep 5
      - echo se durmio 5 seg
      - curl http://44.203.62.27:9000/api/qualitygates/project_status?projectKey=my_zoom > analysis.json
      - cat analysis.json
      - echo Installing source dependencies SONAR...
      - if [ $(jq -r '.projectStatus.status' analysis.json) = ERROR ] ; then $CODEBUILD_BUILD_SUCCEEDING -eq 0 ;fi
      - echo Running unit tests
      #- cd /opt/sonar-scanner-4.7.0.2747-linux/bin
      #- sonar-scanner -Dsonar.organization=My_Zoom -Dsonar.projectKey=My_Zoom -Dsonar.sources=/home/ec2-user/myzoom/src -Dsonar.host.url=http://34.234.167.196:9000 -Dsonar.login=e55ecff6feb353ce0ba7e82f2499f58f82137017
      #- sleep 5
      #- curl http://34.234.167.196:9000/api/qualitygates/project_status?projectKey=My_Zoom > analysis.json
      #- cat analysis.json
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - '**/*'
